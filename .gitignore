# deps
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env*
!.env.example

# typescript
*.tsbuildinfo
next-env.d.ts

# vercel
.vercel

# claude code local tooling (machine-specific paths)
.claude/

# videos — masters are hundreds of MB, far past what git (or a Vercel bundle)
# should carry. Components probe with HEAD (useVideoAvailable) so a checkout
# without them simply doesn't render the player.
/public/videos
/public/testimonials